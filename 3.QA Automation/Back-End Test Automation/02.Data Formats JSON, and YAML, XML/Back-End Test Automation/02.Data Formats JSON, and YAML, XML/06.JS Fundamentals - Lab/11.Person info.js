function personInfo(name1, name2, number){
    let person = {
        firstName : name1,
        lastName : name2,
        age : number
    }

   return person;
}
personInfo(ivan, ivanov, 13);
